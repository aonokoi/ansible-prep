---
- name: Installation of Apache
  hosts: staging_servers
  become: yes

  tasks:
  - name: Install Apache WebServer
    apt:
      name: apache2
      state: latest


  - name: Start Apache WebServer and make enabled
    service:
      name: apache2
      state: started
      enabled: yes

